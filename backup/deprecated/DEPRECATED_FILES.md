# Deprecated / Legacy Items (archived)

This folder contains workspace artifacts and older copies of files archived for inspection and to avoid accidental deletion.

Files archived in this commit:

- `HomePageFixed.txt` — previously-used homepage component variant (copied from repo root).
- `tmp_homepage.html` — temporary homepage HTML (empty file archived).
- `PropertyVideo.txt` — small React component prototype for property video player.
- `stdout.txt` — build/lint stdout log snapshot.
- `upload-queue.json` — old upload queue snapshot.

Notes:
- These files were copied into `backup/deprecated/` (no deletions performed).
- If you want me to remove these from their original locations, tell me which ones and I will move them instead of copying.
- I did a first pass; there may be more legacy artifacts (logs, temp files) — I can continue the sweep on request.
