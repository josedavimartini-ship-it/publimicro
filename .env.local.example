# Example .env.local for local development
# Copy this file to .env.local and fill with your real values.
# Do NOT commit .env.local — it's already ignored by .gitignore.

# Public Supabase client variables (used at build/runtime by Next.js)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optionally, server/service keys (do NOT store these in client-visible .env.local)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Add other local-only variables below
