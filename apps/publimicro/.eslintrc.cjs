// Migrated to root flat ESLint config (eslint.config.js).
// This file left intentionally empty to avoid legacy "extends" usage
// being picked up alongside the flat config. See root `eslint.config.js`.
module.exports = {};
